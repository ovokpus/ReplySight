[phases.setup]
nixPkgs = ['python3', 'pip']

[phases.install]
cmds = ['pip install -r ../requirements.txt']

[phases.build]
cmds = ['echo "Build completed"']

[start]
cmd = 'python -m uvicorn api.app:app --host 0.0.0.0 --port $PORT' 