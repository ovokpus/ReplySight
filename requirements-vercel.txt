# Core dependencies for Vercel serverless functions
# Optimized for faster cold starts and smaller bundle sizes

# Data validation and serialization
pydantic==2.5.0
pydantic-settings==2.1.0

# LangChain ecosystem (core only)
langchain==0.1.0
langgraph==0.0.40
langsmith==0.0.70
langchain-core==0.1.0

# OpenAI for LLM calls
openai==1.3.0

# HTTP client for API calls
httpx==0.25.2

# Environment configuration
python-dotenv==1.0.0

# Async support
asyncio

# JSON handling (built-in)
json

# Type hints
typing-extensions==4.8.0

# Tavily API client (lightweight)
tavily-python==0.2.0 